# Runtime deps for the service. Pinned-ish for reproducible builds - the same
# versions build in CI and in the Docker image.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.5
# Optional: only needed if you set TLDR_MODEL=gemini. The offline default never
# imports it. Kept here so the image can do real summaries when a key is set.
litellm>=1.40
